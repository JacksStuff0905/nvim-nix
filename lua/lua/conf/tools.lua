-- Setup telescope
require('telescope').setup {
  -- defaults = {
    -- find_command = { "fd", "--type", "f", "--type", "l" },
    -- file_ignore_patterns = { ".git/", "node_modules/" },
  -- }
}



